#!/usr/bin/node
const newNum = Math.floor(Number(process.argv[2]));
if (isNaN(newNum)) {
  console.log('Missing number of occurrences');
} else {
  for (let i = 0; i < x; i++) {
    console.log('C is fun');
  }
}
